% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_lsea.R
\name{export_lsea}
\alias{export_lsea}
\title{Export easyLSEA results to disk}
\usage{
export_lsea(
  result,
  dir = ".",
  prefix = "easyLSEA",
  format = c("csv", "excel", "pdf"),
  overwrite = FALSE,
  plot_width = NULL,
  plot_height = NULL,
  plot_dpi = 300L,
  verbose = TRUE
)
}
\arguments{
\item{result}{An \code{easyLSEA_result} object returned by
\code{\link{easyLSEA}}, or a named list with elements \code{lsea}
and/or \code{chains} (output of \code{output = "separate"}).}

\item{dir}{Character(1). Base directory where the output folder will be
created. Default: current working directory (\code{"."}).}

\item{prefix}{Character(1). Prefix for the output folder name. The folder
is named \code{<prefix>_<case>_vs_<ref>_<YYYY-MM-DD_HHMM>/} when
comparison labels are available, otherwise
\code{<prefix>_<YYYY-MM-DD_HHMM>/}. Default: \code{"easyLSEA"}.}

\item{format}{Character vector. One or more of \code{"csv"},
\code{"excel"}, \code{"pdf"}, \code{"png"}, \code{"html"}.
Default: \code{c("csv", "excel", "pdf")}.}

\item{overwrite}{Logical(1). If \code{TRUE}, an existing output folder with
the same name is overwritten. Default: \code{FALSE}.}

\item{plot_width}{Numeric(1) or \code{NULL}. Plot width in inches. If
\code{NULL} (default), width is auto-sized based on the number of lipid
sets in each plot. Pass a number to override for all plots.}

\item{plot_height}{Numeric(1) or \code{NULL}. Plot height in inches. If
\code{NULL} (default), height is auto-sized per plot type. Pass a number
to override for all plots.}

\item{plot_dpi}{Integer(1). Resolution for PNG output. Default: \code{300L}.}

\item{verbose}{Logical(1). Print progress messages. Default: \code{TRUE}.}
}
\value{
Invisibly returns a named character vector of all file paths
created. Useful for programmatic use or verification.
}
\description{
Saves the contents of an \code{\link{easyLSEA}} result object to a
timestamped output folder. Supported formats: CSV tables, a multi-sheet
Excel workbook, PDF or PNG plots, and a standalone HTML report.
Any combination of formats can be requested in a single call.
}
\details{
\subsection{Output folder structure}{

\preformatted{
<prefix>_<YYYY-MM-DD>/
  tables/
    lsea_results_ks.csv
    lsea_results_fgsea.csv
    lsea_combined.csv
    chain_results.csv
    chain_parsed.csv
  plots/
    lsea/
      bubble_ks.pdf
      bubble_fgsea.pdf
    chains/
      tile/
        tile_PC.pdf
        tile_TG.pdf  ...
      trend/
        trend_length_PC.pdf
        trend_unsat_PC.pdf  ...
  results.xlsx
  report.html
}
}

\subsection{Dependencies for optional formats}{

Excel export requires \pkg{openxlsx} (\code{install.packages("openxlsx")}).
HTML export requires \pkg{rmarkdown} and \pkg{knitr}.
}
}
\examples{
data("lipid_example", package = "easyLSEA")

result <- suppressWarnings(suppressMessages(easyLSEA(
  data      = lipid_example,
  engine    = "ks",
  n_perm    = 100L,
  plots     = FALSE,
  verbose   = FALSE
)))

\dontrun{
# Export CSV and PDF to a temp folder
paths <- export_lsea(result, dir = tempdir(), format = c("csv", "pdf"))
paths
}

}
\seealso{
\code{\link{easyLSEA}}, \code{\link{run_lsea}},
\code{\link{parse_lipid_chains}}
}
